# TODO:

1. clone repo
2. execute setup.sh
    1. this adds symbolic links to every config
    2. this also adds scripts to the script directory `~/.local/bin/scripts`
    3. use scripts just out of your terminal
    4. ???
    5. profit

2. add new scripts by
    1. execute `newscript <filename>`
    2. add whatever code you need
    3. save
    4. enjoy
