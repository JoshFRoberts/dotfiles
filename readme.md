# TODO:

1. Place repo in ~/.local/bin
    1. add `export PATH="$PATH:${$(find ~/.local/bin -type d -printf %p:)%%:}` to ~/.zshrc
    2. Use scripts just out of your terminal
    3. ???
    4. Profit

2. add new scripts by
    1. adding a shebang to newly created files
        1. #!/bin/bash
    2. do script stuff
    
